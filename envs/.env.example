# ===== 环境配置说明 =====
#
# 配置已拆分为 3 个文件，按优先级加载（后者覆盖前者）：
#
#   1. envs/local.env 或 envs/ecs.env  —— 环境相关配置（已提交 Git）
#   2. .env.secrets                     —— 密钥（不提交，见 .env.secrets.example）
#   3. .env                             —— 可选的本地覆盖（不提交）
#
# 快速开始：
#   cp .env.secrets.example .env.secrets
#   # 编辑 .env.secrets 填入真实密钥
#
# 切换到 ECS 环境：
#   export APP_ENV=ecs
#
# 代理配置（仅 ECS）：
#   见 envs/mihomo.yaml.README
