# ===== .env 本地覆盖文件 =====
#
# 此文件用于临时覆盖 local.env / ecs.env 中的某个配置值，
# 不提交 Git。加载优先级最高，会覆盖同名变量。
#
# 加载顺序（后者覆盖前者）：
#   1. envs/local.env 或 envs/ecs.env   ← 环境配置（已提交）
#   2. .env.secrets                      ← 密钥（不提交）
#   3. .env                              ← 本地覆盖（不提交，即本文件）
#
# 示例：临时切换调试日志级别
#   LOG_LEVEL=DEBUG
#
# 通常不需要此文件，留空即可。
